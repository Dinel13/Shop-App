class CartItem {
    constructor(quantity, proPrice, proTitle, sum) {
        this.quantity = quantity
        this.proPrice = proPrice
        this.proTitle = proTitle,
        this.sum = sum
    }
}

export default CartItem